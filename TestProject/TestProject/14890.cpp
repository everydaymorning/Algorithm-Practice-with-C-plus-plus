#include <bits/stdc++.h>
using namespace std;
int n, l;
int MAP[101][101];
bool check[101][101];

int main() {
	cin >> n >> l;
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < n; j++) {
			cin >> MAP[i][j];
		}
	}

	for (int i = 0; i < n - l; i++) {
		for (int j = 0; j < n - l; j++) {
			
		}
	}

}